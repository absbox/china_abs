[project]
name = "china-abs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "china_model",
    "absbox.cloud",
    "toMarkdown",
    "docToCloud",
    "scheduler",
    "dashboard",
    "assembler",
]
